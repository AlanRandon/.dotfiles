$mainMod = SUPER # "Windows" key

bind = $mainMod, Return, exec, $terminal
bind = $mainMod, B, exec, $browser
bind = $mainMod, D, exec, fuzzel
bind = $mainMod, E, exec, ~/scripts/hypr-utils pick-emoji
bind = $mainMod, N, exec, $terminal -e impala
bind = $mainMod, V, exec, $terminal -e pulsemixer
bind = $mainMod SHIFT, T, exec, ~/scripts/hypr-utils toggle-touchpad
bind = $mainMod SHIFT, B, exec, $terminal -e ~/scripts/run-bluetuith
bind = $mainMod CTRL, F, exec, ~/scripts/hypr-utils find-window

bind = $mainMod, Space, exec, sh -c 'waybar -c ~/.config/waybar/overlay.jsonc & pid=$! && sleep 2 && kill $pid'

bind = $mainMod, W, exec, ~/scripts/hypr-utils random-wallpaper
bind = $mainMod SHIFT, W, exec, ~/scripts/hypr-utils choose-wallpaper

bind = $mainMod, F, fullscreen
bind = $mainMod SHIFT, Q, killactive,

bind = $mainMod SHIFT, Space, togglefloating

$up = J
$down = K
$left = H
$right = L

bind = $mainMod, $left, movefocus, l
bind = $mainMod, $right, movefocus, r
bind = $mainMod, $up, movefocus, u
bind = $mainMod, $down, movefocus, d

bind = $mainMod SHIFT, $left, swapwindow, l
bind = $mainMod SHIFT, $right, swapwindow, r
bind = $mainMod SHIFT, $up, swapwindow, u
bind = $mainMod SHIFT, $down, swapwindow, d

bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Exit submap
$submapExit = exit (q)uit/(s)hutdown/(l)ock/(h)ibernate/(r)eboot
bind = $mainMod SHIFT, E, submap, $submapExit
submap = $submapExit
bind = , Q, exit,
bind = , S, exec, shutdown -h now
bind = , R, exec, reboot
bind = , L, exec, hyprctl dispatch submap reset && hyprlock
bind = , H, exec, hyprctl dispatch submap reset && hyprlock & sleep 1 && systemctl suspend-then-hibernate
bind = , escape, submap, reset
bind = , Return, submap, reset
bind = , catchall, submap, reset
submap = reset


# Resize submap
$submapResize = resize
bind = $mainMod, R, submap, $submapResize
submap = $submapResize
binde = , $right, resizeactive, 10 0
binde = , $left, resizeactive, -10 0
binde = , $up, resizeactive, 0 -10
binde = , $down, resizeactive, 0 10
bind = , escape, submap, reset
bind = , Return, submap, reset
bind = , catchall, submap, reset
submap = reset

bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Media submap
$submapMedia = media
bind = $mainMod, M, submap, $submapMedia
submap = $submapMedia
bind = , Space, exec, playerctl play-pause
bind = , $left, exec, playerctl position 5-
bind = , $right, exec, playerctl position 5+
bind = , n, exec, playerctl next
bind = , p, exec, playerctl previous
bind = , f, exec, hyprctl dispatch submap reset && ~/scripts/music-playlist-fuzzy
bind = , escape, submap, reset
bind = , Return, submap, reset
bind = , catchall, submap, reset
submap = reset

$roundSink = pactl get-sink-volume @DEFAULT_SINK@ | awk 'NR==1{print(sprintf("%.0f",$5/5)*5)"%"}' | xargs pactl set-sink-volume @DEFAULT_SINK@
binde = , XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5% && $roundSink
binde = , XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5% && $roundSink
binde = , XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle

$roundSource = pactl get-source-volume @DEFAULT_SOURCE@ | awk 'NR==1{print(sprintf("%.0f",$5/5)*5)"%"}' | xargs pactl set-source-volume @DEFAULT_SOURCE@
binde = $mainMod, XF86AudioRaiseVolume, exec, pactl set-source-volume @DEFAULT_SOURCE@ +5% && $roundSource
binde = $mainMod, XF86AudioLowerVolume, exec, pactl set-source-volume @DEFAULT_SOURCE@ -5% && $roundSource
binde = $mainMod, XF86AudioMute, exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle

bind = , XF86AudioPlay, exec, playerctl play-pause
bind = , XF86AudioPrev, exec, playerctl previous
bind = , XF86AudioNext, exec, playerctl next

binde = , XF86MonBrightnessUp, exec, brightnessctl set 5%+
binde = , XF86MonBrightnessDown, exec, brightnessctl set 5%-

bind = , Print, exec, ~/scripts/screenshot screen
bind = $mainMod, Print, exec, ~/scripts/screenshot window
bind = $mainMod SHIFT, Print, exec, ~/scripts/screenshot select

bind = CTRL SHIFT, P, exec, sh -c '[[ "$(hyprctl -j activewindow | jq -r .class)" != "firefox" ]] && wtype -M ctrl -M shift p'
