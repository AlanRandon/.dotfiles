source ~/.config/neomutt/keys/unbinds
source ~/.config/neomutt/keys/binds

source ~/.config/neomutt/accounts/outlook
# folder-hook "accounts/outlook" "source ~/.config/neomutt/accounts/outlook"

source ~/.config/neomutt/catpuccin

set mailcap_path = ~/.config/neomutt/mailcap
auto_view text/html                                   # view HTML automatically
alternative_order text/plain text/enriched text/html  # save HTML for last

set abort_key = "<Esc>"

# set sidebar_visible
# set sidebar_format = "%B%<F? [%F]>%* %<N?%N/>%S"
# set mail_check_stats
